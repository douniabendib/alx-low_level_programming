#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
void _puts(char *s);
_puts("\"Programming is like building a multilingual puzzle");
return (0);
}
